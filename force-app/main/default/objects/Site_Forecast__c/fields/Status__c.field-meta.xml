<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Status__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
AND(
NOT(ISBLANK(Soft_Lock_List__r.Name)),
ISBLANK(Hard_Lock_List__r.Name),
ISBLANK(Final_List__r.Name)
),
TEXT(Soft_Lock_List__r.Status__c),
IF(
AND(
NOT(ISBLANK(Soft_Lock_List__r.Name)),
NOT(ISBLANK(Hard_Lock_List__r.Name)),
ISBLANK(Final_List__r.Name)
),
TEXT(Hard_Lock_List__r.Status__c),
IF(
AND(
NOT(ISBLANK(Soft_Lock_List__r.Name)),
NOT(ISBLANK(Hard_Lock_List__r.Name)),
NOT(ISBLANK(Final_List__r.Name))
),
TEXT(Final_List__r.Status__c),
&quot;None&quot;
)))</formula>
    <label>Status</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
